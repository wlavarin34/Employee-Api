# Employee-Api
The Employee Directory API is a simple, production-ready backend service built with Node.js and Express that allows teams to manage employee information in a structured and scalable way.
